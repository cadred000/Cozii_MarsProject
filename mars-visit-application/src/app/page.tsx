import MultiStageForm from './components/MultiStageForm';

// Add a comment to indicate the correct path to the component
// import MultiStageForm from '../components/MultiStageForm';

const Home = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100">
      <MultiStageForm />
    </div>
  );
};

export default Home;